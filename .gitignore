node_modules/
dist/
coverage/
.env
.env.local
.DS_Store

# Stray logs, but KEEP the workshop's example CI logs (they're teaching fixtures)
*.log
!examples/*.log

# Agent run artifacts (traces written during demos)
.agent-runs/

# Presenter deck & talk text — published separately later, not part of the hands-on repo
presentation.html
assets/
workshop_v2.md

# Local loop-demo run outputs (regenerated by ./loop.sh)
loop_*/

# Prototype marp deck — superseded by the main deck, published separately
slides/
